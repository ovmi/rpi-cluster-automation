- name: Setup ssh on localhost and distribute keys to cluster nodes
  import_tasks: controller_keys.yml
  when: inventory_hostname in groups['local']

- name: Install SSH key and configure passwordless SSH
  import_tasks: nodes_sshd.yml
  when: inventory_hostname in groups['cluster']