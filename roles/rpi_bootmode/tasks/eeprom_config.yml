- name: Ensure rpi-eeprom tools present
  ansible.builtin.package:
    name: rpi-eeprom
    state: present

- name: Process EEPROM configuration per node
  ansible.builtin.include_tasks: eeprom_node.yml
  loop: "{{ nodes_list }}"
  loop_control:
    loop_var: node
